{
  "name": "claudeview",
  "version": "0.1.0",
  "private": true,
  "description": "A project consciousness for Claude Code — observes what actually happened, renders it for humans, serves it to agents.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "tsx": "^4.23.0",
    "typescript": "^5.7.2"
  }
}
