{
  "name": "@claudeview/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": { "claudeview-server": "./dist/server.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx watch src/server.ts",
    "test": "echo no tests"
  },
  "dependencies": {
    "@claudeview/core": "workspace:*"
  },
  "devDependencies": { "@types/node": "^24.0.0", "typescript": "^5.7.2", "tsx": "^4.23.0" }
}
