{
  "name": "@claudeview/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test \"src/**/*.test.ts\""
  },
  "pinNote": "tree-sitter-wasms and web-tree-sitter are pinned EXACTLY. The prebuilt grammars are compiled against one specific web-tree-sitter WASM ABI; a floating range silently loads a runtime that cannot open them, and the indexer then finds zero components without throwing. Do not relax these to ^ ranges.",
  "dependencies": {
    "tree-sitter-wasms": "0.1.13",
    "web-tree-sitter": "0.25.10",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.2"
  }
}
